{
  "name": "shopify-mini-app",
  "version": "1.0.0",
  "description": "Shopify Shop Mini with Supabase backend",
  "private": true,
  "scripts": {
    "start": "cd backend && npm start",
    "build": "cd backend && npm ci --omit=dev",
    "dev": "cd backend && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}